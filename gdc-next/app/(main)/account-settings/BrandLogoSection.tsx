// UNUSED. The business logo is managed on the planner editor and the contract
// section now, both writing users.contract_logo_url. This file was an earlier
// approach (a logo card in account settings) that we backed out of. It's left
// as an empty stub so it type-checks cleanly and can't break the build; nothing
// imports it. Safe to delete from the repo whenever.
export default function BrandLogoSection() {
  return null;
}
